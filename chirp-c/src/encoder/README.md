# Encoder

Native Mimi/Qwen codec encoder code lives in `tools/qwen3-tts-codec` for now and is
linked by v2 when needed. This folder is reserved for the v2-owned split once the
codec code is moved instead of shared.
